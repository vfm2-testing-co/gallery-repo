name: simple workflow

on:
    push:
        branches: ['main']

jobs:
    install:
        runs-on: ubuntu-latest

        steps:
            - name: checkout 
              uses: actions/checkout@v3

            - name: Install dependencies
              run: npm install
               
